---
description: "코드베이스 분석 — 구조, 엔트리포인트, 의존성을 빠르게 요약합니다"
agent: build
---

`$ARGUMENTS`

Treat the text above as the full argument payload for `/auto-map`.
Reinterpret it as `/auto map ...` payload로 다시 해석하고, `skill` 도구로 `auto`를 로드한 뒤 canonical router 규칙을 따르세요.
Preserve `--model <provider/model>` and `--variant <value>` when present.
Do not restate or expand the arguments unless needed for execution.
