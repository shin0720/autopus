# Signature Map

> Auto-generated by `auto setup`. Do not edit manually.

Module: `github.com/shin0720/auto-adk`

## pkg/config

- `func DefaultFullConfig(projectName string) (*HarnessConfig)` — DefaultFullConfig returns the default config for Full mode.
@AX:NOTE: [AUTO] magic constants — model names (opus, sonnet, haiku, gpt-5.4, gemini-3.1-pro-preview), timeouts, and tier mappings are hardcoded below

- `func Load(dir string) (*HarnessConfig, error)` — Load는 autopus.yaml을 로드한다.

- `func Save(dir string, cfg *HarnessConfig) (error)` — Save validates and writes the config to autopus.yaml.

- `func MigrateOrchestraConfig(cfg *HarnessConfig) (bool, error)` — MigrateOrchestraConfig performs all orchestra config migrations.
It returns (changed bool, err error).

Migrations applied:
 1.

- `func EnsureOrchestraProvider(cfg *HarnessConfig, providerName string) (error)` — EnsureOrchestraProvider ensures a specific provider exists in the orchestra config.
This is used by the "platform add" command to keep orchestra config consistent.

- `func ProviderToPlatform(name string) (string)` — ProviderToPlatform maps common provider/binary names to valid platform names.
Used to normalize user input that confuses provider names with platform names.
Returns "" when no mapping is needed (name is already a valid platform name or unknown).

- `func MigratePlatformNames(cfg *HarnessConfig) (bool)` — MigratePlatformNames normalizes provider names mistakenly used as platform names.
Returns true if any platform name was corrected.

- `func PlatformToProvider(platform string) (string)` — PlatformToProvider maps platform names to orchestra provider names.
@AX:NOTE: [AUTO] "opencode" maps to "codex" — intentional alias per SPEC-ORCHCFG-002 migration

- `func MigrateOpencodeToCodex(cfg *HarnessConfig) (bool, error)` — MigrateOpencodeToCodex replaces opencode provider entries with codex.
Removes opencode from providers and commands, adds codex if not already present.
Returns (changed bool, err error).
@AX:NOTE [AUTO]: One-way migration per SPEC-ORCHCFG-002 — opencode entries are permanently replaced with codex

- `type PlatformSettings struct` — PlatformSettings is the canonical cross-platform settings representation.
Each platform adapter can serialize from this struct into its native format.

- `type AgentDef struct` — AgentDef is a platform-agnostic agent definition.

- `type RuleDef struct` — RuleDef is a platform-agnostic rule definition.

- `type SkillDef struct` — SkillDef is a platform-agnostic skill definition.

- `type HookDef struct` — HookDef is a platform-agnostic hook definition.

- `func (ps *PlatformSettings) ToClaudeJSON() ([]byte, error)` — ToClaudeJSON serializes to Claude's nested JSON format (.claude/settings.json style).

- `func (ps *PlatformSettings) ToGeminiJSON() ([]byte, error)` — ToGeminiJSON serializes to Gemini's flat JSON format (.gemini/settings.json style).

- `func (ps *PlatformSettings) ToCodexConfig() ([]byte, error)` — ToCodexConfig serializes the agent portion to Codex TOML format.
Codex uses TOML for agents, JSON for hooks, and MD for AGENTS.md.
This method produces the TOML agent config portion.

- `func ParseClaudeJSON(data []byte) (*PlatformSettings, error)` — ParseClaudeJSON deserializes Claude's nested JSON format back to PlatformSettings.

- `func ParseGeminiJSON(data []byte) (*PlatformSettings, error)` — ParseGeminiJSON deserializes Gemini's flat JSON format back to PlatformSettings.

- `func ParseCodexConfig(data []byte) (*PlatformSettings, error)` — ParseCodexConfig deserializes Codex TOML agent config back to PlatformSettings.
Only agents are parsed (Codex TOML only contains agent definitions).

- `type Mode struct` — Mode는 설치 모드를 나타낸다.

- `type LanguageConf struct` — LanguageConf는 프로젝트 언어 설정이다.

- `type QualityPreset struct` — QualityPreset defines a named quality configuration with agent mappings.

- `type QualityConf struct` — QualityConf holds quality preset definitions and the default preset name.

- `type SkillsConf struct` — SkillsConf holds configuration for the skills activation system.

- `type IssueReportConf struct` — IssueReportConf is the auto issue reporter configuration.

- `type HarnessConfig struct` — HarnessConfig는 autopus.yaml의 최상위 설정 구조이다.

- `type ProfilesConf struct` — ProfilesConf holds profile configuration for agents.

- `type ExecutorProfileConf struct` — ExecutorProfileConf holds executor profile settings.

- `type OrchestraConf struct` — OrchestraConf는 다중 모델 오케스트레이션 설정이다 (Full 전용).

- `type SubprocessConf struct` — SubprocessConf holds global subprocess execution settings.

- `type ProviderEntry struct` — ProviderEntry는 프로바이더 실행 설정이다.

- `type SubprocessProvConf struct` — SubprocessProvConf holds per-provider subprocess execution overrides.

- `type CommandEntry struct` — CommandEntry는 커맨드별 오케스트레이션 설정이다.

- `type ArchitectureConf struct` — ArchitectureConf는 ARCHITECTURE.md 설정이다.

- `type LoreConf struct` — LoreConf는 Lore Decision Knowledge 설정이다.

- `type SpecConf struct` — SpecConf는 SPEC 엔진 설정이다.

- `type ReviewGateConf struct` — ReviewGateConf는 멀티-프로바이더 SPEC 리뷰 게이트 설정이다.

- `type MethodologyConf struct` — MethodologyConf는 방법론 설정이다 (Full 전용).

- `type RouterConf struct` — RouterConf는 Category-based 모델 라우팅 설정이다 (Full 전용).

- `type HooksConf struct` — HooksConf는 훅 설정이다.

- `type PermissionsConf struct` — PermissionsConf는 코딩 CLI 권한 설정이다.

- `type SessionConf struct` — SessionConf는 세션 연속성 설정이다 (Full 전용).

- `type VerifyConf struct` — VerifyConf is the frontend UX verification configuration.

- `type ConstraintConf struct` — ConstraintConf is the anti-pattern constraint configuration.

- `type ContextConf struct` — ContextConf is the agent context enrichment configuration.

- `func (c *HarnessConfig) Validate() (error)` — Validate는 설정의 유효성을 검증한다.

- `func (c *HarnessConfig) IsFullMode() (bool)` — IsFullMode는 Full 모드 여부를 반환한다.

- `type UsageProfile struct` — UsageProfile represents how the user intends to use ADK.

- `func DefaultUsageProfile() (UsageProfile)` — DefaultUsageProfile returns the default profile for backward compatibility.

- `func (p UsageProfile) IsValid() (bool)` — IsValid checks if the profile value is recognized.

- `func (p UsageProfile) Effective() (UsageProfile)` — Effective returns the effective profile, defaulting empty to developer.

- `type HintsConf struct` — HintsConf holds configuration for non-intrusive platform hints.

- `func (h HintsConf) IsPlatformHintEnabled() (bool)` — IsPlatformHintEnabled returns whether platform hints are active.

## pkg/adapter

- `interface PlatformAdapter` — PlatformAdapter는 코딩 CLI 플랫폼별 어댑터 인터페이스이다.

- `type PlatformFiles struct` — PlatformFiles는 어댑터가 생성한 파일 목록이다.

- `type FileMapping struct` — FileMapping은 단일 파일 매핑이다.

- `type OverwritePolicy struct` — OverwritePolicy는 파일 덮어쓰기 정책이다.

- `type ValidationError struct` — ValidationError는 검증 에러이다.

- `type HookConfig struct` — HookConfig는 훅 설정이다.

- `type PermissionSet struct` — PermissionSet는 플랫폼별 권한 설정이다.

- `type Manifest struct` — Manifest는 autopus-adk가 관리하는 파일 목록이다.

- `type ManifestFile struct` — ManifestFile은 매니페스트에 기록된 단일 파일 정보이다.

- `type UpdateAction struct` — UpdateAction은 업데이트 시 파일별 처리 결과이다.

- `type UpdateResult struct` — UpdateResult는 단일 파일의 업데이트 결과이다.

- `func NewManifest(platform string) (*Manifest)` — NewManifest는 빈 매니페스트를 생성한다.

- `func ManifestFromFiles(platform string, pf *PlatformFiles) (*Manifest)` — ManifestFromFiles는 PlatformFiles로부터 매니페스트를 생성한다.

- `func LoadManifest(root, platform string) (*Manifest, error)` — LoadManifest는 프로젝트 루트에서 매니페스트를 로드한다.
매니페스트가 없으면 nil을 반환한다 (에러 아님).

- `func (m *Manifest) Save(root string) (error)` — Save는 매니페스트를 디스크에 저장한다.

- `func ResolveAction(root string, targetPath string, policy OverwritePolicy, old *Manifest) (UpdateAction)` — ResolveAction은 파일별 업데이트 액션을 결정한다.

규칙:
  - 파일 없음 + 매니페스트에 있음 → 사용자가 삭제함 → Skip
  - 파일 없음 + 매니페스트에 없음 → 새 파일 → Create
  - 파일 있음 + 체크섬 동일 → 사용자 미수정 → Overwrite
  - 파일 있음 + 체크섬 다름 → 사용자 수정함 → Backup
  - 마커 정책 파일 → 항상 Overwrite (마커 섹션만 교체하므로 안전)

- `func BackupFile(root, targetPath, backupDir string) (string, error)` — BackupFile은 파일을 백업 디렉터리로 복사한다.

- `func CreateBackupDir(root string) (string, error)` — CreateBackupDir은 타임스탬프 기반 백업 디렉터리를 생성한다.

- `func Checksum(s string) (string)` — Checksum은 문자열의 SHA256 체크섬을 반환한다.

- `type Registry struct` — Registry는 PlatformAdapter 레지스트리이다.

- `func NewRegistry() (*Registry)` — NewRegistry는 빈 레지스트리를 생성한다.

- `func (r *Registry) Register(a PlatformAdapter)` — Register는 어댑터를 등록한다.

- `func (r *Registry) Get(name string) (PlatformAdapter, error)` — Get은 이름으로 어댑터를 조회한다.

- `func (r *Registry) List() ([]PlatformAdapter)` — List는 등록된 모든 어댑터를 반환한다.

- `func (r *Registry) DetectAll(ctx context.Context) ([]PlatformAdapter)` — DetectAll은 설치된 코딩 CLI를 감지하여 해당 어댑터만 반환한다.

- `func WriteFileIfChanged(path string, content []byte, perm os.FileMode) (error)` — WriteFileIfChanged writes content to path only when the existing file content
differs.

## pkg/terminal

- `type CmuxAdapter struct` — CmuxAdapter implements Terminal using the cmux terminal multiplexer.

- `func (a *CmuxAdapter) Name() (string)` — Name returns the adapter name.

- `func (a *CmuxAdapter) CreateWorkspace(_ context.Context, name string) (error)` — CreateWorkspace creates a new cmux workspace and renames it to the given name.
It stores the workspace ref internally for use by Close.

- `func (a *CmuxAdapter) CreateSurface(_ context.Context) (PaneID, error)` — CreateSurface creates a new independent surface (tab) via cmux new-surface.
Each surface gets the full terminal width, avoiding pane width starvation.

- `func (a *CmuxAdapter) SplitPane(_ context.Context, dir Direction) (PaneID, error)` — SplitPane creates a new split pane in the given direction and returns its surface ref.

- `func (a *CmuxAdapter) SendCommand(_ context.Context, paneID PaneID, command string) (error)` — SendCommand sends a command string to the specified pane via --surface flag.

- `func (a *CmuxAdapter) SendLongText(ctx context.Context, paneID PaneID, text string) (error)` — SendLongText sends text to a pane.

- `func (a *CmuxAdapter) Notify(_ context.Context, message string) (error)` — Notify sends a notification message via cmux notify --title.

- `func (a *CmuxAdapter) Close(_ context.Context, name string) (error)` — Close closes a surface or workspace by ref or stored workspace name.
If name is a cmux ref (surface:N or pane:N), uses close-surface.
If name is a workspace ref (workspace:N), uses close-workspace.
Otherwise, uses the stored workspaceRef from CreateWorkspace.

- `func (a *CmuxAdapter) ReadScreen(_ context.Context, paneID PaneID, opts ReadScreenOpts) (string, error)` — ReadScreen reads pane content via cmux read-screen.

- `func (a *CmuxAdapter) PipePaneStart(_ context.Context, paneID PaneID, outputFile string) (error)` — PipePaneStart starts streaming pane output to a file via cmux pipe-pane.

- `func (a *CmuxAdapter) PipePaneStop(_ context.Context, paneID PaneID) (error)` — PipePaneStop stops pipe-pane output streaming via empty command.

- `func (a *CmuxAdapter) SurfaceHealth(_ context.Context, paneID PaneID) (SurfaceStatus, error)` — SurfaceHealth checks surface health via `cmux surface-health`.
Output format: "surface:7 type=terminal in_window=true"

- `func (a *CmuxAdapter) WaitForSignal(ctx context.Context, name string, timeout time.Duration) (error)` — WaitForSignal blocks until the named signal is received via `cmux wait-for`.
Uses exec.CommandContext to respect the provided timeout.

- `func (a *CmuxAdapter) SendSignal(_ context.Context, name string) (error)` — SendSignal sends a named signal via `cmux wait-for -S`.

- `func DetectTerminal() (Terminal)` — DetectTerminal returns the best available terminal adapter.
Priority: cmux > tmux > plain.
@AX:ANCHOR [AUTO] high fan-in entry point — called by 6 CLI command handlers in terminal_cmd.go
@AX:REASON: changes to detection priority (cmux > tmux > plain) affect all terminal subcommands; coordinate with terminal_cmd.go callers before modifying

- `type PlainAdapter struct` — PlainAdapter implements Terminal as a no-op fallback when no multiplexer is available.

- `func (a *PlainAdapter) Name() (string)` — Name returns the adapter name.

- `func (a *PlainAdapter) CreateWorkspace(_ context.Context, _ string) (error)` — CreateWorkspace is a no-op that logs a warning.

- `func (a *PlainAdapter) SplitPane(_ context.Context, _ Direction) (PaneID, error)` — SplitPane is a no-op.

- `func (a *PlainAdapter) SendCommand(_ context.Context, _ PaneID, _ string) (error)` — SendCommand is a no-op.

- `func (a *PlainAdapter) SendLongText(_ context.Context, _ PaneID, _ string) (error)` — SendLongText is a no-op.

- `func (a *PlainAdapter) Notify(_ context.Context, _ string) (error)` — Notify is a no-op.

- `func (a *PlainAdapter) ReadScreen(_ context.Context, _ PaneID, _ ReadScreenOpts) (string, error)` — ReadScreen is a no-op that returns an empty string.

- `func (a *PlainAdapter) PipePaneStart(_ context.Context, _ PaneID, _ string) (error)` — PipePaneStart is a no-op.

- `func (a *PlainAdapter) PipePaneStop(_ context.Context, _ PaneID) (error)` — PipePaneStop is a no-op.

- `func (a *PlainAdapter) Close(_ context.Context, _ string) (error)` — Close is a no-op.

- `type SurfaceStatus struct` — SurfaceStatus represents the health status of a terminal surface.

- `interface SignalCapable` — SignalCapable is an optional interface for terminal adapters that support
signal-based communication.

- `type Direction struct` — Direction represents the split direction for pane creation.

- `type PaneID struct` — PaneID is the identifier for a terminal pane.

- `type ReadScreenOpts struct` — ReadScreenOpts configures ReadScreen behavior.

- `interface SurfaceCreator` — SurfaceCreator is an optional interface for terminals that support independent
surfaces (tabs) instead of splits.

- `interface Terminal` — Terminal is the unified interface for terminal multiplexer adapters.
@AX:ANCHOR [AUTO] core public API contract — all adapters (cmux, tmux, plain) implement this interface
@AX:REASON: any method signature change here breaks all three adapters and every CLI handler that calls them; treat as a stable boundary

- `type TmuxAdapter struct` — TmuxAdapter implements Terminal using the tmux terminal multiplexer.

- `func (a *TmuxAdapter) Name() (string)` — Name returns the adapter name.

- `func (a *TmuxAdapter) CreateWorkspace(_ context.Context, name string) (error)` — CreateWorkspace creates a new tmux session or window.
When running inside an existing tmux session (TMUX env set), creates a new window
instead to avoid nested session errors.

- `func (a *TmuxAdapter) SplitPane(_ context.Context, dir Direction) (PaneID, error)` — SplitPane splits the current pane horizontally or vertically.

- `func (a *TmuxAdapter) SendCommand(_ context.Context, paneID PaneID, command string) (error)` — SendCommand sends a shell command to the specified pane.

- `func (a *TmuxAdapter) Notify(_ context.Context, message string) (error)` — Notify displays a message in the tmux status bar.

- `func (a *TmuxAdapter) ReadScreen(_ context.Context, paneID PaneID, opts ReadScreenOpts) (string, error)` — ReadScreen reads pane content via tmux capture-pane.

- `func (a *TmuxAdapter) PipePaneStart(_ context.Context, paneID PaneID, outputFile string) (error)` — PipePaneStart starts streaming pane output to a file via tmux pipe-pane.

- `func (a *TmuxAdapter) PipePaneStop(_ context.Context, paneID PaneID) (error)` — PipePaneStop stops pipe-pane output streaming via no-arg pipe-pane.

- `func (a *TmuxAdapter) SendLongText(_ context.Context, paneID PaneID, text string) (error)` — SendLongText sends text to a pane, using load-buffer/paste-buffer for text >= 500 bytes
to avoid tmux send-keys truncation.

- `func (a *TmuxAdapter) Close(_ context.Context, name string) (error)` — Close kills the named tmux session.

## pkg/detect

- `type Platform struct` — Platform은 감지된 코딩 CLI 정보이다.

- `func DetectPlatforms() ([]Platform)` — DetectPlatforms는 PATH에서 코딩 CLI를 감지한다.

- `func IsInstalled(binary string) (bool)` — @AX:ANCHOR [AUTO]: Do not rename or change the signature of IsInstalled
@AX:REASON: Called by 6+ consumers — doctor, doctor_fix, spec_review, verify, orchestra, detect internals
IsInstalled는 특정 바이너리의 설치 여부를 확인한다.

- `type OrchestraProvider struct` — OrchestraProvider represents an orchestra provider and its install status.

- `func DetectOrchestraProviders() ([]OrchestraProvider)` — DetectOrchestraProviders checks which orchestra provider binaries are installed.

- `func InstalledOrchestraProviders() ([]string)` — InstalledOrchestraProviders returns only the installed orchestra providers.

- `type Dependency struct` — Dependency는 외부 도구 의존성이다.

- `func (d Dependency) IsNpmBased() (bool)` — IsNpmBased reports whether this dependency is installed via npm.
@AX:NOTE [AUTO] public method with single call site; add test coverage for non-npm prefix cases

- `func CheckDependencies(deps []Dependency) ([]DependencyStatus)` — CheckDependencies는 의존성 상태를 확인한다.

- `type DependencyStatus struct` — DependencyStatus는 의존성 상태이다.

- `type ParentRuleConflict struct` — ParentRuleConflict는 부모 디렉터리에서 발견된 규칙 충돌 정보이다.

- `func CheckParentRuleConflicts(projectDir string) ([]ParentRuleConflict)` — CheckParentRuleConflicts는 부모 디렉터리에 상속될 .claude/rules/ 네임스페이스가 있는지 탐지한다.
Claude Code는 상위 디렉터리를 탐색하며 규칙을 로드하므로,
부모에 어떤 규칙 네임스페이스라도 있으면 현재 프로젝트에 의도치 않게 중첩 적용될 수 있다.

- `type Framework struct` — Framework represents a detected project framework.

- `func DetectFramework(dir string) (*Framework, error)` — DetectFramework detects the framework used in the given project directory.
Returns nil if no known framework is detected.
@AX:ANCHOR [AUTO] @AX:REASON: public API — called by setup and planner for profile assignment; do not change signature

- `func DetectStack(dir string) (string, error)` — DetectStack detects the primary project stack from directory signals.
@AX:NOTE [AUTO] @AX:REASON: public API boundary — R2 stack detection; priority order matters (go > ts > python > rust)

- `type PermissionResult struct` — PermissionResult holds the detected permission mode and metadata.

- `func DetectPermissionMode() (PermissionResult)` — DetectPermissionMode checks the parent process tree for the
--dangerously-skip-permissions flag and returns the permission mode.
Priority: AUTOPUS_PERMISSION_MODE env > process tree scan > cmux heuristic.
cmux launches Claude Code without visible CLI flags, so process tree scan
always returns "safe".

- `func DetectTestRunner(dir string) (string, error)` — DetectTestRunner detects the test runner used in the given project directory.
Returns the runner name ("vitest", "jest", "go test", "pytest", "cargo test")
or an empty string if no known test runner is found.

## internal/cli/tui

- `func Banner(w io.Writer)` — Banner prints the Autopus brand banner with version info.

- `func BannerWithInfo(w io.Writer, project, mode string)` — BannerWithInfo prints the banner with project context.

- `func SectionHeader(w io.Writer, title string)` — SectionHeader prints a styled section header.

- `func Divider(w io.Writer)` — Divider prints a thin muted divider line.

- `func Octopus(w io.Writer)` — Octopus prints the brand sign-off emoji.

- `func Box(w io.Writer, title, content string)` — Box prints content inside a rounded branded box.

- `func InfoBox(w io.Writer, title, content string)` — InfoBox prints content inside a blue info box.

- `func ErrorBox(w io.Writer, title, content string)` — ErrorBox prints content inside a red error box.

- `func ResultBox(w io.Writer, passed bool, message string)` — ResultBox prints a summary result box (e.g., doctor diagnosis).

- `func DetectSystemLang() (string)` — DetectSystemLang returns the best-matching language code (en, ko, ja, zh)
based on the system locale environment variables.
Falls back to "en" if no supported locale is detected.

- `func Success(w io.Writer, msg string)` — Success prints a green success message.

- `func Successf(w io.Writer, format string, a ...any)` — Successf prints a formatted green success message.

- `func Error(w io.Writer, msg string)` — Error prints a red error message.

- `func Errorf(w io.Writer, format string, a ...any)` — Errorf prints a formatted red error message.

- `func Warn(w io.Writer, msg string)` — Warn prints a yellow warning message.

- `func Warnf(w io.Writer, format string, a ...any)` — Warnf prints a formatted yellow warning message.

- `func Info(w io.Writer, msg string)` — Info prints a blue info message.

- `func Infof(w io.Writer, format string, a ...any)` — Infof prints a formatted blue info message.

- `func Step(w io.Writer, current, total int, msg string)` — Step prints a progress step indicator.

- `func Bullet(w io.Writer, msg string)` — Bullet prints a bulleted item.

- `func Tag(label, value string) (string)` — Tag prints a styled inline tag (e.g., mode, platform name).

- `func OK(w io.Writer, msg string)` — OK prints a compact OK status for checklist items.

- `func FAIL(w io.Writer, msg string)` — FAIL prints a compact FAIL status for checklist items.

- `func SKIP(w io.Writer, msg string)` — SKIP prints a compact WARN status for checklist items.

- `func EnsureSafeEnv()`

- `func InitStyles()` — InitStyles initializes all styles.

- `type SummaryRow struct` — SummaryRow represents a key-value pair for the summary table.

- `func WizardHeader(w io.Writer, step, total int, title string)` — WizardHeader prints a step counter combined with a section header.

- `func SummaryTable(w io.Writer, rows []SummaryRow)` — @AX:NOTE [AUTO]: SummaryTable uses bannerWidth (40) for box width — long values will wrap
SummaryTable prints a key-value summary inside a branded box.

- `type InitWizardOpts struct` — InitWizardOpts holds options for the init wizard.

- `type InitWizardResult struct` — InitWizardResult holds all wizard selections.

- `func RunInitWizard(opts InitWizardOpts) (*InitWizardResult, error)` — RunInitWizard runs the interactive init wizard using huh forms.
When opts.Accessible is true, it returns defaults without launching a TUI.

- `func AutopusTheme() (*huh.Theme)` — AutopusTheme returns a custom huh theme using Autopus brand colors.
R4: Focused option highlighted with ColorViolet (#7c3aed).
R8: Uses only colors defined in style.go.

## pkg/learn

- `func Prune(store *Store, days int) (int, error)` — Prune removes entries older than the given number of days.
Returns the number of entries removed.

- `func MatchRelevance(entry LearningEntry, query RelevanceQuery) (float64)` — MatchRelevance scores how relevant an entry is to the given query.
Returns 0.0 if no match.

- `func QueryRelevant(store *Store, query RelevanceQuery, threshold float64) ([]LearningEntry, error)` — QueryRelevant returns entries with relevance score above threshold,
sorted by score descending.

- `func RecordGateFail(store *Store, opts RecordOpts) (error)` — RecordGateFail records a gate failure learning entry.

- `func RecordCoverageGap(store *Store, opts RecordOpts) (error)` — RecordCoverageGap records a coverage gap learning entry.

- `func RecordReviewIssue(store *Store, opts RecordOpts) (error)` — RecordReviewIssue records a review issue learning entry.

- `func RecordExecutorError(store *Store, opts RecordOpts) (error)` — RecordExecutorError records an executor error learning entry.

- `func RecordFixPattern(store *Store, opts RecordOpts) (error)` — RecordFixPattern records a fix pattern learning entry.

- `type Store struct` — Store manages learning entries in a JSONL file.

- `func NewStore(dir string) (*Store, error)` — NewStore creates a store rooted at dir, ensuring .autopus/learnings/ exists.

- `func (s *Store) Append(entry LearningEntry) (error)` — Append adds a new entry to the JSONL file.

- `func (s *Store) Read() ([]LearningEntry, error)` — Read reads all entries from the JSONL file.
Returns empty slice if the file does not exist.

- `func (s *Store) NextID() (string, error)` — NextID generates the next L-{NNN} ID based on existing entries.

- `func (s *Store) AppendAtomic(entryType EntryType, opts RecordOpts) (error)` — AppendAtomic atomically generates an ID and appends a new learning entry.
It holds a mutex to prevent race conditions between NextID and Append.

- `func (s *Store) UpdateReuseCount(id string) (error)` — UpdateReuseCount increments reuse_count for the entry with the given ID.

- `func GenerateSummary(store *Store, topN int) (Summary, error)` — GenerateSummary generates a learning summary from the store.
topN limits the number of top patterns returned.

- `type EntryType struct` — EntryType represents the category of a learning entry.

- `type Severity struct` — Severity represents the impact level of a learning entry.

- `type LearningEntry struct` — LearningEntry represents one learning record (R12).

- `func IsValidEntryID(id string) (bool)` — IsValidEntryID checks whether id matches the L-{NNN} format.

- `type RelevanceQuery struct` — RelevanceQuery holds parameters for relevance matching (R13).

- `type Summary struct` — Summary holds learning summary for sync display (R8).

- `type RecordOpts struct` — RecordOpts holds options for recording a learning entry.

- `type PatternStat struct` — PatternStat tracks reuse frequency of a pattern.

<!-- Filtered: 52 packages omitted (low fan-in) -->
