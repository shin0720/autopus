# Architecture

## Overview

Architecture analysis not available. Run `auto arch generate` to create a detailed analysis.

### Go Standard Layout

- `cmd/` → Application entry points (highest level)
- `internal/` → Private packages (not importable by external code)
- `pkg/` → Public reusable libraries

