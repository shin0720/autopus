# E2E Scenarios — [Project Name]

> Auto-generated by `/auto setup`. Updated by `/auto sync`.
> Template: scenarios-api (API server applications)

## Project Type: API
## Binary: [binary name or N/A for interpreted languages]
## Build: [build command or N/A]

<!--
Each scenario represents a single API endpoint interaction.
Scenario ID format: {method}-{resource} or {method}-{resource}-{variant}
Verification primitives available:
  status_code(N), json_path(path, value), header_contains(key, str),
  response_time_ms(N), exit_code(N), stdout_contains(str)

Detection signals (Go): go.mod + net/http, gin, chi, echo, fiber
Detection signals (Python): pyproject.toml + flask, fastapi, django
Detection signals (Node): package.json + express, fastify, nestjs, hono
-->

### S1: [method]-[resource] — [description, e.g., "GET /api/users returns user list"]
- **Command**: `curl -s -X GET http://localhost:[port]/api/[resource]`
- **Precondition**: [e.g., "server running on :8080, seed data loaded"]
- **Env**: [e.g., API_KEY=test-key or N/A]
- **Expect**: [e.g., "200 OK, JSON array of users"]
- **Verify**: status_code(200), json_path($.length, >0)
- **Depends**: N/A
- **Status**: active

### S2: [method]-[resource]-auth — [auth error scenario]
- **Command**: `curl -s -X GET http://localhost:[port]/api/[resource]`
- **Precondition**: [e.g., "server running, no auth token"]
- **Env**: N/A
- **Expect**: [e.g., "401 Unauthorized"]
- **Verify**: status_code(401), json_path($.error, Unauthorized)
- **Depends**: N/A
- **Status**: active

### S3: [method]-[resource]-create — [creation scenario]
- **Command**: `curl -s -X POST http://localhost:[port]/api/[resource] -H "Content-Type: application/json" -d '{"key":"value"}'`
- **Precondition**: [e.g., "server running, valid auth token"]
- **Env**: [e.g., AUTH_TOKEN=test-token]
- **Expect**: [e.g., "201 Created, returns created resource"]
- **Verify**: status_code(201), json_path($.id, exists)
- **Depends**: N/A
- **Status**: active

<!--
Add more scenarios following the pattern above.
Guidelines:
- One scenario per route (happy path) + auth/validation error scenarios
- Group by resource: all /users scenarios, then all /posts scenarios
- Chain dependent scenarios: S5 (DELETE) depends on S3 (CREATE) for resource ID
- Include health check endpoint as S1 if available
-->
