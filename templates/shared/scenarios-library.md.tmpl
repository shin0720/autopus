# E2E Scenarios — [Project Name]

> Auto-generated by `/auto setup`. Updated by `/auto sync`.
> Template: scenarios-library (Library / SDK)

## Project Type: Library
## Binary: N/A
## Build: [build command, e.g., `go build ./...` or `npm run build`]

<!--
Each scenario represents a public API usage pattern.
Libraries have no CLI or server entry points, so scenarios test
the primary exported functions/classes from a consumer's perspective.
Scenario ID format: {function}-{variant} or {usecase}
Verification primitives available:
  exit_code(N), stdout_contains(str), stderr_empty(),
  file_exists(path), file_contains(path, str)

Detection: No CLI/API/Frontend signals detected — fallback type.
-->

### S1: [function-or-usecase] — [description, e.g., "Parse valid input returns expected output"]
- **Command**: `[test runner command, e.g., go test -run TestIntegration_Parse ./pkg/parser]`
- **Precondition**: [e.g., "test fixtures in testdata/"]
- **Env**: N/A
- **Expect**: [e.g., "test passes, no errors"]
- **Verify**: exit_code(0), stderr_empty()
- **Depends**: N/A
- **Status**: active

### S2: [function-or-usecase]-error — [error handling scenario]
- **Command**: `[test runner command]`
- **Precondition**: [e.g., "invalid input fixture"]
- **Env**: N/A
- **Expect**: [e.g., "returns descriptive error, no panic"]
- **Verify**: exit_code(0), stdout_contains(PASS)
- **Depends**: N/A
- **Status**: active

<!--
Add more scenarios following the pattern above.
Guidelines:
- Focus on integration-level tests of public API surface
- One scenario per major exported function/type
- Chain scenarios that build on each other (init → configure → execute)
- For SDKs, test the primary "getting started" flow
-->
