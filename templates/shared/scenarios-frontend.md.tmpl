# E2E Scenarios — [Project Name]

> Auto-generated by `/auto setup`. Updated by `/auto sync`.
> Template: scenarios-frontend (Frontend applications)

## Project Type: Frontend
## Binary: N/A
## Build: [build command, e.g., `npm run build`]

<!--
Each scenario represents a user-facing page interaction.
Scenario ID format: {page}-{action} or {page}-{variant}
Verification primitives available:
  page_title(str), element_visible(selector), element_text(selector, str),
  url_matches(pattern), exit_code(N), stdout_contains(str)

Detection signals: package.json + react/vue/svelte/next/nuxt/angular deps
-->

### S1: [page]-load — [description, e.g., "Home page loads successfully"]
- **Command**: `navigate http://localhost:[port]/`
- **Precondition**: [e.g., "dev server running on :3000"]
- **Env**: [e.g., NEXT_PUBLIC_API_URL=http://localhost:8080 or N/A]
- **Expect**: [e.g., "page loads, main heading visible"]
- **Verify**: page_title([expected title]), element_visible([main selector])
- **Depends**: N/A
- **Status**: active

### S2: [page]-interaction — [user interaction scenario]
- **Command**: `click [selector] then wait [condition]`
- **Precondition**: [e.g., "S1 completed, user on home page"]
- **Env**: N/A
- **Expect**: [e.g., "navigation to /dashboard, dashboard content visible"]
- **Verify**: url_matches(/dashboard), element_visible([dashboard selector])
- **Depends**: S1
- **Status**: active

### S3: [page]-form — [form submission scenario]
- **Command**: `fill [form selector] then submit`
- **Precondition**: [e.g., "user on login page"]
- **Env**: [e.g., TEST_USER=admin, TEST_PASS=test123]
- **Expect**: [e.g., "form submits, redirects to dashboard"]
- **Verify**: url_matches(/dashboard), element_text([welcome selector], [expected text])
- **Depends**: S1
- **Status**: active

### S4: [page]-error — [error state scenario]
- **Command**: `navigate http://localhost:[port]/nonexistent`
- **Precondition**: [e.g., "dev server running"]
- **Env**: N/A
- **Expect**: [e.g., "404 page displayed"]
- **Verify**: element_visible([error selector]), element_text([error selector], [expected text])
- **Depends**: N/A
- **Status**: active

<!--
Add more scenarios following the pattern above.
Guidelines:
- One scenario per page/route (load + key interaction)
- Cover critical user flows: login, main action, error states
- Chain navigation flows: S1 (home) → S2 (navigate) → S3 (action)
- For SPAs, test client-side routing separately
-->
