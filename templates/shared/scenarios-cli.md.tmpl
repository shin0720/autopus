# E2E Scenarios — [Project Name]

> Auto-generated by `/auto setup`. Updated by `/auto sync`.
> Template: scenarios-cli (CLI applications)

## Project Type: CLI
## Binary: [binary name, e.g., `./bin/myapp`]
## Build: [build command, e.g., `go build -o bin/myapp ./cmd/myapp`]

<!--
Each scenario represents a single user-facing CLI interaction.
Scenario ID format: {subcommand} or {subcommand}-{variant}
Verification primitives available:
  exit_code(N), stdout_contains(str), stderr_empty(),
  file_exists(path), file_contains(path, str)

Detection signals (Go): go.mod + github.com/spf13/cobra
Detection signals (Python): pyproject.toml/setup.py + argparse/click/typer
Detection signals (Node): package.json + commander/yargs/oclif
-->

### S1: [command-id] — [description of what the command does]
- **Command**: `[binary] [subcommand] [flags/args]`
- **Precondition**: [required state, e.g., "config file exists at ~/.myapp/config.yaml"]
- **Env**: [KEY=value or N/A]
- **Expect**: [expected result, e.g., "exits 0, prints version string"]
- **Verify**: exit_code(0), stdout_contains([expected output])
- **Depends**: N/A
- **Status**: active

### S2: [command-id]-error — [error scenario description]
- **Command**: `[binary] [subcommand] [invalid flags/args]`
- **Precondition**: N/A
- **Env**: N/A
- **Expect**: [expected error, e.g., "exits 1, prints usage help"]
- **Verify**: exit_code(1), stdout_contains(Usage)
- **Depends**: N/A
- **Status**: active

<!--
Add more scenarios following the pattern above.
Guidelines:
- One scenario per subcommand (happy path) + one error scenario
- For subcommands with multiple modes, add variants: S3: init-verbose, S4: init-force
- Chain dependent scenarios: S5 depends on S3 if it needs S3's output
- Mark deprecated scenarios as Status: deprecated (do not delete)
-->
